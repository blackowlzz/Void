# Configurația Void — https://github.com/blackowlzz/Void
# Copyright 2025 MWHunter (DefineOutside), blackowlzz și colaboratorii, licențiat sub GPLv3.

# ─── General ─────────────────────────────────────────────────────────────────

alerts:
    # Trimite alertele de violare și în consolă pe lângă stafful online.
    print-to-console: true
    proxy:
        # Redirecționează alertele către alte servere din proxy (BungeeCord/Velocity).
        send: false
        receive: false

verbose:
    print-to-console: false

check-for-updates: true
experimental-checks: false

client-brand:
    # Listă regex de clienți al căror brand este ignorat silențios.
    ignored-clients:
        - "^vanilla$"
        - "^fabric$"
        - "^quilt$"
        - "^lunarclient:v\\d+\\.\\d+\\.\\d+-\\d{4},(?:fabric|forge)$"
        - "^Feather Fabric$"
        - "^labymod$"
    # Deconectează clienții Forge 1.18.2–1.19.3 cu reach hacks integrate.
    disconnect-blacklisted-forge-versions: true

spectators:
    # Ascunde spectatorii cu permisiunea void.spectator chiar dacă nu spectează activ.
    hide-regardless: false
    # Restrânge ascunderea la lumi specifice. Gol = toate lumile.
    allowed-worlds:
        - ""

# Secunde înainte de a kicca un jucător care nu mai răspunde.
max-transaction-time: 60

# Cheie API Pastebin pentru /void debug. Gol = salvare locală.
pastebin-api-key: ""

# ─── Verificări ──────────────────────────────────────────────────────────────

Simulation:
    setback-decay-multiplier: 0.999
    threshold: 0.001
    immediate-setback-threshold: 0.1
    max-advantage: 1
    max-ceiling: 4
    setback-violation-threshold: 1

Knockback:
    setback-decay-multiplier: 0.999
    threshold: 0.001
    immediate-setback-threshold: 0.1
    max-advantage: 1
    max-ceiling: 4

Explosion:
    threshold: 0.001
    setbackvl: 3

Phase:
    setbackvl: 1
    decay: 0.005

NoSlow:
    threshold: 0.001
    setbackvl: 5
    decay: 0.05

Reach:
    threshold: 0.0005
    block-impossible-hits: true
    enable-post-packet: false

TimerA:
    setbackvl: 10
    drift: 120

TimerLimit:
    ping-abuse-limit-threshold: 1000

NegativeTimer:
    drift: 1200

VehicleTimer:
    setbackvl: 10

AirLiquidPlace:
    cancelvl: 0

FabricatedPlace:
    cancelvl: 5

FarPlace:
    cancelvl: 5

PositionPlace:
    cancelvl: 5

RotationPlace:
    cancelvl: 5

PacketOrderI:
    # Activează dacă jucătorii cu moduri de animație 1.7 sunt marcați greșit.
    exempt-placing-while-digging: false

exploit:
    allow-sprint-jumping-when-using-elytra: true
    allow-building-on-ghostblocks: true
    distance-to-check-if-ghostblocks: 2

    auto-totem:
        autopunish:
            enabled: false
            action: ban
            duration: "1d"
            reason: |
                &cAutoTotem detectat.
                &7Check: &f%check_name% &8(&dVL %vl%&8)
            # Dacă true, comanda de mai jos este executată prin consolă în loc de banul intern.
            prefer-custom-ban: false
            # Șablon comandă. Marcatori: {player}  {duration}  {reason}
            # LiteBans:    "litebans:tempban {player} {duration} {reason}"
            # AdvancedBan: "ban {player} {reason} -t {duration}"
            # EssentialsX: "ban {player} {reason}"
            custom-ban-command: "litebans:tempban {player} {duration} {reason}"

# ─── Pedepse ─────────────────────────────────────────────────────────────────

punishments:

    auto-punish:
        enabled: false
        threshold: 10
        action: ban
        duration: "7d"
        reason: |
            &5VoidAC
            &cCont Suspendat
            &7Ban ID: &d%ban_id%
            &7Durată: &f%duration%
        support-link: ""
        broadcast-message: "&8[&5Void&8] &f%player% &ca fost banat &7pentru joc neloial."
        # Dacă true, comanda de mai jos este executată prin consolă în loc de banul intern.
        prefer-custom-ban: false
        # Șablon comandă. Marcatori: {player}  {duration}  {reason}
        # LiteBans:    "litebans:tempban {player} {duration} {reason}"
        # AdvancedBan: "ban {player} {reason} -t {duration}"
        # EssentialsX: "ban {player} {reason}"
        custom-ban-command: "litebans:tempban {player} {duration} {reason}"

    manual-punish:
        duration: "7d"
        reason: |
            &5VoidAC
            &cCont Suspendat
            &7Banat de: &f{issued_by}
            &7Ban ID: &d{ban_id}
            &7Durată: &f{duration}
        prefer-custom-ban: false
        custom-ban-command: "litebans:tempban {player} {duration} {reason}"

# ─── Valul de Banuri ─────────────────────────────────────────────────────────
# /void banwave add <player> [duration]  — adaugă în coadă
# /void banwave execute                  — banează pe toți acum

ban-wave:
    enabled: true
    queue-on-auto-punish: false
    auto-execute-days: 7
    duration: "7d"
    reason: |
        &5VoidAC
        &cCont Suspendat
        &7Valul de Banuri: &d#{wave}
        &7Ban ID: &d{ban_id}
        &7Durată: &f{duration}
    broadcast: "&8[&5Void&8] &7Valul de Banuri &d#{wave} &7executat — &d{count} &7cheateri eliminați."
    prefer-custom-ban: false
    # Șablon comandă. Marcatori: {player}  {duration}  {reason}
    # LiteBans:    "litebans:tempban {player} {duration} {reason}"
    # AdvancedBan: "ban {player} {reason} -t {duration}"
    custom-ban-command: "litebans:tempban {player} {duration} {reason}"

# ─── Storage ESP Decoys ───────────────────────────────────────────────────────

storage-esp-decoys:
    enabled: false
    decoys-per-chunk: 20
    include-client-side-blocks: false
    auto-flag-threshold: 16

# ─── Anti-Xray ───────────────────────────────────────────────────────────────

AntiXray:
    configure-paper: false
    engine-mode: 2
    max-block-height: 320
    update-radius: 16
    hidden-blocks:
        - air
        - copper_ore
        - deepslate_copper_ore
        - raw_copper_block
        - iron_ore
        - deepslate_iron_ore
        - gold_ore
        - deepslate_gold_ore
        - diamond_ore
        - deepslate_diamond_ore
        - emerald_ore
        - deepslate_emerald_ore
        - lapis_ore
        - deepslate_lapis_ore
        - redstone_ore
        - deepslate_redstone_ore
        - coal_ore
        - deepslate_coal_ore
        - nether_quartz_ore
        - nether_gold_ore
        - ancient_debris
        - chest
        - trapped_chest
        - barrel
        - ender_chest
        - shulker_box
        - white_shulker_box
        - orange_shulker_box
        - magenta_shulker_box
        - light_blue_shulker_box
        - yellow_shulker_box
        - lime_shulker_box
        - pink_shulker_box
        - gray_shulker_box
        - light_gray_shulker_box
        - cyan_shulker_box
        - purple_shulker_box
        - blue_shulker_box
        - brown_shulker_box
        - green_shulker_box
        - red_shulker_box
        - black_shulker_box
    replacement-blocks:
        - stone
        - deepslate
        - granite
        - diorite
        - andesite
        - dirt
        - gravel
        - tuff
        - calcite
        - smooth_basalt
        - coal_ore
        - deepslate_coal_ore

# ─── Optimizatorul de Praguri ─────────────────────────────────────────────────

threshold-optimizer:
    enabled: false

# ─── Avansat ─────────────────────────────────────────────────────────────────

packet-spam-threshold: 100
debug-packet-cancel: false

max-ping-out-of-flying: 1000
max-ping-firework-boost: 1000

disable-pong-cancelling: false
disable-default-resync-handler: false

cancel-duplicate-packet: true
ignore-duplicate-packet-rotation: false

reset-item-usage-on-item-update: true
reset-item-usage-on-attack: true
reset-item-usage-on-slot-change: true
reset-item-usage-on-item-use: true

debug-pipeline-on-join: false

config-flavor: V2
config-version: 19
